# 수시 지원 도우미 — 통합 사이트 사용법

`index.html` **한 파일**에 선생님용·학생용이 함께 들어 있습니다.
로그인한 사람이 누구냐에 따라 화면이 달라집니다.

---

## 처음 한 번만 — `추가기능.sql` 실행

이번 판부터 **설정 화면에서 교직원 계정을 만들고, 학생·교직원 비밀번호를 되돌리고, 계정을 삭제할 수** 있습니다.
(계정 삭제는 성적·지원계획까지 지우며 되돌릴 수 없습니다 — 전학 등으로 나간 학생, 학교를 떠난 선생님에게 쓰세요.)
그 일은 데이터베이스가 해야 해서, 저장소의 **`추가기능.sql`** 을 한 번만 실행해 주세요.

1. Supabase → 프로젝트 → **SQL Editor**
2. `추가기능.sql` 을 통째로 붙여넣고 **[Run]**

여러 번 실행해도 안전합니다. 실행하지 않으면 그 단추를 눌렀을 때
「아직 데이터베이스에 없습니다 — 추가기능.sql 을 실행해 주세요」 라고 알려 줍니다.
나머지 기능(전형 찾기 · 달력 · 엑셀 올리기 · 지원계획)은 실행하지 않아도 그대로 됩니다.

> `추가기능.sql` 은 이미 있는 `create_teacher()` 를 그대로 부릅니다.
> 실행했을 때 「function create_teacher(...) does not exist」 가 나오면
> 처음 설치할 때 쓴 SQL 을 먼저 넣어 주세요.

---

## 지금 바로 로그인할 수 있는 계정

시작할 수 있도록 **총관리자 계정을 하나 만들어 두었습니다.**

| | |
|---|---|
| 아이디 | `admin` (또는 `admin@susi.local` 전체로 넣어도 됩니다) |
| 비밀번호 | `susi2027admin` |
| 학교 이름 | 우리학교 |

> **이 비밀번호는 반드시 바꾸세요.** 이 문서에 적혀 있으니 그대로 두면 안 됩니다.
> Supabase → SQL Editor 에서:
> ```sql
> update auth.users
>    set encrypted_password = extensions.crypt('새_비밀번호', extensions.gen_salt('bf', 10))
>  where email = 'admin@susi.local';
> ```
> 학교 이름도 같은 곳에서 바꿉니다: `update schools set name = '○○고등학교';`

### 담임 선생님 계정 (3학년 1~9반)

**9개를 이미 만들어 두었습니다.** 각 담임은 **자기 반 학생만** 보입니다.

| 아이디 | 담당 | 초기 비밀번호 |
|---|---|---|
| `teacher1` | 3학년 1반 | `1111` |
| `teacher2` | 3학년 2반 | `1111` |
| … | … | … |
| `teacher9` | 3학년 9반 | `1111` |

**초기 비밀번호는 9명 모두 `1111` 입니다.**
처음 들어가면 **비밀번호 정하는 화면이 먼저 뜨고**, 새로 정해야 안으로 들어갑니다(8자 이상).

> `1111` 은 누구나 아는 값입니다. 바꾸기 전까지는 옆자리 선생님도 그 계정으로 들어와
> 담당 학생 자료를 볼 수 있으니, **받으시면 바로 들어가서 바꾸도록** 안내해 주세요.

나중에 다시 바꾸고 싶으면 화면 오른쪽 위 **[비밀번호]** 를 누르면 됩니다.
비밀번호를 잊으셨으면 총관리자가 **설정 → 교직원 계정 → [비밀번호 초기화]** 를 눌러
초기 비밀번호로 되돌려 줍니다(그 뒤 다시 새로 정하게 됩니다).

> **로그인 화면에는 초기 비밀번호를 적어 두지 않습니다.** 어깨너머로 보이기 때문입니다.
> 초기 비밀번호는 **설정 화면에서 계정을 만들 때 한 번** 보여 드리니 그때 적어 전해 주세요.

담임과 총관리자는 이렇게 다릅니다.

| | 담임 | 총관리자 |
|---|---|---|
| 보이는 학생 | 배정된 반만 | 학교 전체 |
| 엑셀로 계정 만들기 | ✗ | ✓ |
| 지원계획 고치기 | 자기 반만 | 전체 |
| 담당 반 지정 | ✗ | ✓ (지금은 SQL 로) |

담당 반을 바꾸려면:

```sql
delete from teacher_classes
 where teacher_id = (select id from auth.users where email='teacher3@susi.local');
select assign_teacher_class(
  (select id from auth.users where email='teacher3@susi.local'), 3::smallint, 5::smallint);
```

### 로그인이 안 될 때

로그인에 실패하면 메시지 아래에 **「보낸 아이디」**가 같이 나옵니다.
거기 적힌 값이 `admin@susi.local` 이 아니라면 아이디가 잘못 만들어져 나간 것이니
**아이디 칸에 `admin@susi.local` 전체를 넣어** 보세요.

그래도 안 되면 Supabase → **Authentication → Providers → Email** 에서
**Confirm email 이 꺼져 있는지** 확인해 주세요.

---

## 선생님 계정 더 만들기

**총관리자로 들어가 [설정] → [교직원 계정 만들기]** 에서 **이름만 넣고 [만들기]** 를 누르면 됩니다.
(`추가기능.sql` 을 실행해 두셔야 합니다.)

- **아이디는 자동으로 지어집니다** — `teacher10`, `teacher11` … 비어 있는 첫 번호를 씁니다.
  직접 정하고 싶으면 아이디 칸에 넣으세요(영문 소문자·숫자 2~40자).
- **담당** 에서 「일반 / 3학년 N반 담임 / 총관리자」 를 바로 고를 수 있습니다.
  나중에 아래 **교직원 계정** 에서 바꿔도 됩니다.
- 만들고 나면 **아이디와 초기 비밀번호가 화면에 한 번** 나옵니다.
  **그때 적어 두고 전해 주세요** — 화면을 옮기면 다시 볼 수 없습니다.
- 그 선생님이 처음 들어가면 **비밀번호를 새로 정하는 화면**이 먼저 뜹니다.

### SQL 로 만들고 싶다면

1. Supabase → 프로젝트 → **SQL Editor**
2. 담임 선생님을 만들고 담당 반을 정하려면

```sql
do $$
declare v_school uuid; v_t uuid;
begin
  select id into v_school from schools limit 1;
  v_t := create_teacher(v_school, 'teacher1', '김담임', '여기에_긴_비밀번호', false);
  insert into teacher_classes (teacher_id, school_id, grade, class_no)
  values (v_t, v_school, 3, 1);          -- 3학년 1반
end $$;
```

> **총관리자**는 학교 전체를 보고 학생 명부를 올릴 수 있습니다.
> **담임**은 맡은 반만 봅니다(명부 등록은 못 합니다).

> 아이디는 **`teacher1` 처럼 짧게** 지으면 됩니다. 뒤에 `@susi.local` 이 자동으로 붙어
> Supabase 인증이 쓰는 이메일 모양이 됩니다(학번을 `30101@susi.local` 로 쓰는 것과 같은 방식).
> 화면에는 아이디만 보입니다. 비밀번호는 **8자 이상**이어야 합니다.

3. Supabase → **Authentication → Providers → Email** 에서
   **Confirm email 을 꺼 주세요.** 학번으로 만든 가짜 이메일이라 확인 메일이 갈 곳이 없습니다.

---

## 화면 구성 (메뉴)

| 누가 | 보이는 메뉴 |
|---|---|
| **학생** | **전형 찾기** · **전형 관리** |
| **담임** | **학생** · **전형 찾기** · **수시 달력** |
| **총관리자** | 위의 셋 + **설정** (교직원·학생 계정 · 엑셀 올리기 · 데이터 갈아끼우기 · 리셋) |

> **데이터**(그 해 전형자료를 통째로 갈아끼우는 화면)는 **총관리자에게만** 보입니다.
> 담임이 실수로 누르면 학교 전체의 자료가 같이 바뀌기 때문입니다.

---

## 선생님이 하는 일

`수시도우미_2027.html` 을 열고 **[선생님]** 탭에서 **아이디·비밀번호**로 들어갑니다.
위에서 만든 아이디(`admin` `teacher1` 같은 것)를 그대로 넣으면 됩니다.
진짜 이메일 주소로 계정을 만들었다면 그 주소를 넣어도 됩니다.

### ① 엑셀 올리기 → 학생 자료

**`학생자료양식.xlsx`** 를 그대로 올리시면 됩니다. 이 한 파일로
**계정 만들기 · 내신 · 모의고사**가 한 번에 들어갑니다.

머리글이 두 줄인 양식을 그대로 읽습니다.

| 1행 (묶음) | | | 국수영과 | 국수영사 | 전과목 | 3월 · 5월 · 6월 · 7월 · 9월 · 수능 | | |
|---|---|---|---|---|---|---|---|---|
| **2행** | 반 | 번호 · 이름 | 1학년 · 2학년 · 3학년 · 평균 | 〃 | 〃 | 국 · 수 · 영 · 탐1 · 탐2 | 생년월일 | 전화번호 |

- **학년**은 화면에서 고르고, 학번은 **학년1+반2+번호2** 로 자동으로 만듭니다(3학년 1반 1번 → `30101`).
- **생년월일·전화번호가 있어야 계정이 만들어집니다.** 없는 줄은 계정 없이 성적만 들어갑니다.
- 생년월일은 `20080101` `080101` `2008-01-01` `2008.1.1` `2008년 1월 1일` 다 읽습니다.
- **등급 칸의 `0` 이나 빈칸은 넣지 않고 건너뜁니다**(등급은 1~9만 유효).
- **같은 반·번호가 여러 줄** 있으면 첫 줄만 쓰고 나머지는 건너뛰며 알려 줍니다.
- 이미 있는 학생을 다시 올리면 **이름·연락처와 성적만 갱신되고 계정·비밀번호는 그대로**입니다.
- 올리고 나면 **학생별 첫 비밀번호 목록**이 화면에 나옵니다.

> 계정 만들기는 **총관리자만** 됩니다. 담임 선생님이 올리면 **성적만** 들어갑니다.

#### 성적만 따로 올리기

**생년월일·전화번호 칸을 비우거나 그 열이 아예 없는 파일**을 올리면
계정은 건드리지 않고 **성적만** 들어갑니다. 이미 등록된 학생만 반영되고,
명부에 없는 줄은 건너뛰며 알려 줍니다.

#### 한 명만 손으로 고치기

**학생** 탭에서 학생을 고르고 **[성적 입력]** 을 누르면
내신(3계열 × 1·2·3학년·평균)과 모의고사(3·5·6·7·9월·수능 × 국·수·영·탐1·탐2)를
표에 직접 넣을 수 있습니다. 한 명만 바로잡을 때 엑셀을 다시 올릴 필요가 없습니다.

칸 배치는 **`학생자료양식.xlsx` 와 똑같습니다** — 내신은 `국·수·영·과 / 국·수·영·사 / 전과목`
을 `1학년·2학년·3학년·평균` 으로, 모의고사는 `3월·5월·6월·7월·9월·수능` 을
`국·수·영·탐1·탐2` 로 넣습니다. 엑셀을 보면서 그대로 옮겨 적으면 됩니다.

맨 아래에 **생년월일·전화번호** 칸이 있습니다(엑셀 양식의 마지막 두 칸과 같습니다).
**고치면 그 값이 곧 그 학생의 새 비밀번호**가 됩니다. 둘 다 비우면 등록이 지워져
학생이 다음 로그인 때 다시 넣습니다.

- 비워 두면 「없음」으로 저장됩니다.
- 등급은 **1~9만** 받습니다. 벗어난 값이 있으면 저장하지 않고 알려 줍니다.
- **반은 설정한 반 개수 안에서만** 받습니다. 벗어난 줄은 건너뛰고 알려 줍니다
  (반이 더 필요하면 **설정 → 반 개수**를 먼저 늘리세요).
- **번호는 1~99 까지만** 받습니다. 학번이 `학년1 + 반2 + 번호2` 다섯 자리라
  100번이 되면 자리올림이 나서 **옆 반 학생이 되어 버립니다.** 그래서 막아 둡니다.

### ② 학생

담당 학생 목록과, 학생을 고르면 그 학생의 성적·지원계획이 나옵니다.
**미접속** 배지는 아직 한 번도 로그인하지 않은 학생입니다.

#### 지원계획 표 — 선생님이 바로 고칠 수 있습니다

**총관리자**와 그 반 **담임**은 학생의 지원계획 표를 화면에서 그대로 고칠 수 있습니다.
칸을 고치면 **바로 저장**되고, 표 아래에 「저장됨」이 잠깐 뜹니다.
(고른 칸에서 벗어나거나 잠깐 멈추면 저장합니다. 따로 [저장] 단추는 없습니다.)

| 칸 | 색 | 어디서 오나 |
|---|---|---|
| 구분 / 대학·전형·학과 | 회색 | 학생이 담은 그대로 (여기서는 못 고침) |
| 70%컷 / 모집 / 경쟁률 | 회색 | 전형데이터에서 **그때그때 다시 찾아** 보여 줌 |
| 판정 | 흰색 | 기본은 **자동** — 70%컷과 그 학생 내신을 비교해 상향/적정/하향. 다르게 보시면 직접 고르면 그 값이 이깁니다 |
| 1단계 | 흰색 | **직접 입력** (자료에 없는 항목입니다) |
| 면접 · 논술 | 흰색 | 전형데이터에 날짜가 있으면 **미리 채워져** 나옵니다. 고치면 고친 값이 남고, 그대로 두면 자료가 바뀔 때 같이 따라갑니다 |
| 충원 | 흰색 | **직접 입력** (자료에 없는 항목입니다) |
| 합불 | 흰색 | 미정 / 합격 / 불합격 |

**판정 칸이 「자동」으로만 나오고 값이 없을 때**는 그 옆에 이유가 붙습니다.

- `내신 없음` — 그 학생 성적이 아직 안 올라갔습니다 → **[성적 입력]** 또는 엑셀
- `컷 없음` — 그 전형은 어디가 자료에 합격컷이 없습니다 (신설·소수모집 등)
- `자료에 없음` — 대학·전형·학과 이름이 전형데이터와 안 맞습니다 (이름이 바뀐 경우)

> **1단계·충원은 원래 비어 있는 게 맞습니다.** 어디가 자료에 없는 항목이라 손으로 넣는 칸입니다.
> 면접·논술도 자료에 날짜가 있는 전형만 채워집니다 (교과 전형은 대부분 없습니다).

### ④ 설정 — 총관리자만 보이는 탭

담임 선생님께는 이 탭이 아예 안 보입니다.
**학생 자료 올리기(엑셀)** 와 **데이터(자료 갈아끼우기)** 도 이 탭 안으로 들어왔습니다 —
둘 다 총관리자만 하는 일이라 선생님 화면을 어지럽히지 않게 모아 두었습니다.

#### 반 개수

한 학년에 몇 반까지 있는지 정합니다(1~20). 아래 **교직원 계정**에서 반을 고르는 칸이
이 수만큼 나오고, 엑셀을 올릴 때도 이 범위를 벗어난 반은 걸러 냅니다.
학교 이름도 여기서 바꿉니다.

> **반 개수를 줄이면 사라지는 반의 담임 배정이 함께 지워집니다.**
> 9반에서 7반으로 줄이면 8·9반 담임은 담당 학생이 없는 상태가 됩니다.

#### 교직원 계정 — 역할과 담당 반

**[역할/반 설정]** 을 누르면 각 선생님 아래에 고르는 칸이 펼쳐집니다. 누르면 **바로 저장**됩니다.

| 고르는 것 | 그 선생님이 보는 학생 | 계정 만들기 |
|---|---|---|
| **일반** | 없음 | ✗ |
| **전체** | 학교 전체 (총관리자) | ✓ |
| **301 · 302 …** | 그 반 학생만 (담임) | ✗ |

> **본인 계정은 총관리자에서 못 내려옵니다.** 마지막 관리자가 스스로 내려오면 아무도
> 이 탭에 못 들어가기 때문입니다. 다른 분을 먼저 **전체**로 올린 뒤에 바꾸세요.

**[비밀번호 초기화]** 를 누르면 그 선생님 비밀번호가 **초기 비밀번호로 되돌아가고**,
다음 로그인 때 새로 정하게 됩니다. 되돌린 값은 그 자리에서 알려 드립니다.
본인 계정은 여기서 못 되돌립니다 — 화면 위의 **[비밀번호]** 를 쓰세요.

#### 학생 계정 — 비밀번호 되돌리기

학생 비밀번호는 늘 **생년월일 6자리 + 전화번호 뒤 4자리** 입니다.
학생이 못 들어온다고 하면 이 목록에서 이름이나 학번으로 찾아 —

| 단추 | 하는 일 | 남는 것 |
|---|---|---|
| **[비밀번호 초기화]** | 등록해 둔 생년월일·전화번호로 **비밀번호를 다시 맞춤** | 연락처·성적·지원계획 그대로 |
| **[등록 초기화]** | 생년월일·전화번호를 **지움** → 학생이 다음 로그인 때 다시 넣음 | 성적·지원계획 그대로 |

> 학생이 **연락처를 잘못 넣어** 못 들어오는 경우가 가장 흔합니다.
> 맞는 값을 아신다면 **학생 → [성적 입력]** 화면 맨 아래에서 생년월일·전화번호를
> 바로 고쳐 주는 것이 가장 빠릅니다 (고친 값이 곧 새 비밀번호가 됩니다).

#### 학생 자료 리셋

학생 계정과 그 학생들의 **내신 · 모의고사 · 지원계획**을 모두 지웁니다.
**선생님 계정과 반 설정은 그대로** 둡니다. 새 학년도를 시작할 때 쓰세요.

실수로 눌리지 않게 두 겹으로 막아 두었습니다 —

1. 확인 칸에 **`학생자료삭제`** 를 정확히 쳐야 단추가 열립니다
2. 단추를 눌러도 한 번 더 물어봅니다

**되돌릴 수 없습니다.** 지우기 전에 엑셀로 내려받아 두시길 권합니다.

---

### ③ 수시 달력

**한 화면에 한 달만** 보여 줍니다. 여섯 달을 한꺼번에 늘어놓으면 화면에서도 종이에서도
어느 달도 제대로 안 보이기 때문입니다.

- 위의 **◀ ▶** 로 달을 옮기고, 가운데 고르는 칸으로 바로 건너뜁니다
  (칸에 그 달의 고사 건수가 같이 적혀 있어 어느 달이 바쁜지 한눈에 보입니다).
- 전년 9월부터 그 학년도 2월까지(수시 시즌) 여섯 달이 **고사가 없어도** 늘 있습니다 —
  어느 날이 비어 있는지도 알아야 하는 정보라서요.
- 처음 열면 **오늘이 든 달**이 나옵니다(시즌 밖이면 고사가 처음 있는 달).

달력 머리에는 학교 일정표처럼 **큰 달 숫자 · `2026 3학년 월간 일정표` · 학교 이름** 이 붙습니다.
학교 이름은 **설정 → 학교 이름** 에 넣은 값이 그대로 나옵니다.

#### D-day

각 날짜 칸에 **`수능 D-79`** 처럼 남은 날이 적힙니다. 지나간 일정은 적지 않습니다.

**[D-day 설정]** 을 펴면 —

| 칸 | 기본값 |
|---|---|
| 수능일 | 그 학년도 수능 날짜를 **자동으로 계산** (11월 셋째 목요일) |
| 학사일정 이름 | `중간고사` — 원하는 이름으로 바꾸세요 |
| 그 날짜 | 비어 있음 — 넣으면 그 줄이 같이 나옵니다 |

> 이 값은 **그 선생님의 브라우저에만** 저장됩니다(서버를 건드리지 않습니다).
> 학교 컴퓨터를 바꾸면 한 번 더 넣어 주셔야 합니다.

#### 칸에 얹히는 것

담당 학생들의 지원계획에 들어 있는 **면접·논술·실기** 날짜입니다.
한 줄이 `학생이름 · 대학 · 면접/논술/실기` 이고, 학생은 **학번순**으로 정렬됩니다.

**한 학생이 같은 날 두 곳 이상**이면 그 칸이 빨갛게 되고 날짜 옆에 ⚠ 가 붙습니다.

한 칸에는 **세 건까지만** 적고 나머지는 아래에 **`+10건`** 처럼 접힙니다.
칸 높이는 어떤 경우에도 고정이라 달력 모양이 흐트러지지 않습니다.

**날짜를 누르면** 달력 아래에 그 날 **전체**가 펼쳐집니다.
학생별로 묶여 나오고, 같은 날 두 곳인 학생은 빨갛게 표시됩니다.
한 번 더 누르면 닫힙니다.

#### 휴대폰에서는

칸이 작아 이름을 담을 수 없으므로, **몇 건인지만 동그라미로** 찍습니다
(겹치는 날은 빨간 동그라미). **날짜를 누르면** 아래에 그 날 전체가 펼쳐집니다.
가로로 스크롤할 필요 없이 한 달이 화면에 다 들어옵니다.

#### 인쇄 (A4 세로)

**[이 달 인쇄]** 를 누르면 **보고 있는 달 한 장**이 나옵니다.

- 맨 위에 **누구의 표인지·학교·담당 학생 수·고사 건수·출력한 날짜**가 찍힙니다
- **접힌 것 없이 그 날 전부가 나옵니다** — `+N건` 대신 실제 내용이 펼쳐지고 칸이 늘어납니다
- **고사가 없는 달도 그대로 나옵니다** — 학사 일정표로도 쓰이므로 빈 칸 자체가 정보입니다
- 겹치는 날은 흑백으로 뽑아도 알아볼 수 있게 **굵은 테두리와 ⚠** 로 표시됩니다
- 화면의 단추·메뉴와 아래 펼침판은 인쇄되지 않고 달력만 나옵니다

> 여러 달을 뽑으시려면 달을 옮겨 가며 한 번씩 인쇄하시면 됩니다.

---

## 학생이 하는 일

같은 파일을 열고 **[학생]** 탭에서 —

| 칸 | 넣는 것 |
|---|---|
| 학번 | 5자리 (30101 = 3학년 1반 1번) |
| 생년월일 | 숫자 8자리로 붙여서 — 2008년 1월 1일이면 `20080101` |
| 전화번호 | 010-1234-5678 |

**학생은 비밀번호를 따로 정하지 않습니다.** 늘 이 세 칸으로 들어옵니다.
처음 들어올 때 넣은 생년월일·전화번호가 그대로 그 학생의 정보가 되고,
그때부터 그 두 값이 비밀번호 노릇을 합니다.
잘못 넣어 못 들어오면 **선생님이 설정 화면에서 되돌려** 줄 수 있습니다.

들어오면 메뉴가 둘입니다 —

| 메뉴 | 하는 일 |
|---|---|
| **전형 찾기** | 조건으로 전형을 찾고, 줄 오른쪽 **[담기]** 로 담습니다 |
| **전형 관리** | 담아 둔 것을 **일반대 6칸 · 예비 3칸 · 전문대(담은 만큼)** 로 놓고 관리합니다 |

**입력하는 대로 자동 저장**됩니다(오른쪽 위에 `저장됨` 표시).
다른 기기에서 로그인해도 그대로 있습니다.

### 전형 관리 — 칸 나누기

- **일반대 수시 지원 · 6칸** — 수시 지원은 여섯 곳까지라 칸도 여섯입니다.
- **예비 · 3칸** — 아직 정하지 못한 곳을 담아 둡니다. 카드의 **⇄** 로 지원 칸과 자리를 바꿉니다.
- **전문대** — **칸 수 제한이 없습니다.** 한 칸을 채우면 다음 칸이 저절로 생깁니다
  (전문대는 지원 횟수 제한이 없기 때문입니다).

전형 찾기에서 **[담기]** 를 누르면 —
일반대는 **지원 칸 → 다 차면 예비 칸**에, 전문대는 **전문대 칸**에 저절로 들어갑니다.

---

## ⚠ 꼭 알아 두실 것

학생 비밀번호는 **생년월일 6자리 + 전화번호 뒤 4자리**로 **고정**입니다.
학생이 바꿀 수 없습니다 — 바꿔 버리면 잊었을 때 아무도 못 들어가고
계정을 지웠다 다시 만드는 수밖에 없기 때문입니다.

대신 **먼저 등록한 사람이 임자**인 구조로 막습니다 —

- 명부에는 **학번·이름만** 올라갑니다. 생년월일·전화번호는 **학생이 처음 들어올 때 본인이** 넣습니다.
- 한 번 등록되면 다른 사람은 그 학번으로 못 들어옵니다(선생님 목록에서 **미등록** 배지가 사라집니다).
- 잘못 넣었으면 **설정 → 학생 계정 → [등록 초기화]** 로 되돌려 다시 넣게 합니다.

**학생들에게 되도록 빨리 로그인해 등록하게 해 주세요.**
미등록 목록이 빨리 비는 것이 이 방식에서 가장 중요한 안전장치입니다.

선생님 계정은 반대입니다 — **초기 비밀번호로 만들어지고, 첫 로그인 때 반드시 바꾸게** 합니다.
그 값이 어깨너머로 보이지 않도록 **로그인 화면에는 적어 두지 않습니다.**

---

## 올리는 곳

정적 HTML 파일 하나라 아무 데나 올리면 됩니다 — 학교 홈페이지, Netlify, Vercel, GitHub Pages 등.
파일을 그냥 더블클릭해도 열리지만, **여러 사람이 쓰려면 웹에 올리는 편**이 낫습니다.

접속 정보는 `site/susi_config.js` 에 있고, 파일 안에 함께 들어갑니다.
거기 있는 키는 공개용이라 웹에 있어도 괜찮습니다 — 로그인해야 하고, 로그인해도
접근 규칙이 자기 것만 보여 줍니다. **`service_role` 키는 절대 넣지 마세요.**

---

## 고치고 다시 만들기

```bash
# site/shell.html  site/shell.css  site/shell.js  site/susi_config.js 를 고친 뒤
python3 mk_site.py        # → 수시도우미_2027.html
```

`app.css` `app_markup.html` `app.js` 는 **단독 앱과 함께 쓰는 파일**입니다.
고치면 `mk_standalone.py` `mk_app.py` `mk_site.py` 를 모두 다시 돌리세요.
